name: Labler

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

jobs:
  assign-labels:
    permissions:
      contents: write
      pull-requests: write
      issues: write
    runs-on: ubuntu-latest
    steps:
      - name: PR Conventional Commit Validation1
        uses:  ytanikin/pr-conventional-commits@1.4.0
        with:
          task_types: '["feat","fix","docs","test","ci","refactor","perf","chore","revert"]'

